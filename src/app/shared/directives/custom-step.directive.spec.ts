import { CustomStepDirective } from './custom-step.directive';

describe('CustomStepDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStepDirective();
    expect(directive).toBeTruthy();
  });
});
