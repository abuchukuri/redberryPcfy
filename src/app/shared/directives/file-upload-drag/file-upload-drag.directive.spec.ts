import { FileUploadDragDirective } from './file-upload-drag.directive';

describe('FileUploadDragDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadDragDirective();
    expect(directive).toBeTruthy();
  });
});
